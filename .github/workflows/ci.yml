name: ci

on:
  pull_request:
  push:
    branches: [main]

jobs:
  repo-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Prettier check (md/yml)
        run: npx --yes prettier --check "**/*.{md,yml,yaml}"
